name: daily_quote_app
description: A simple quote app with sharing and copying features.
version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  share_plus: ^7.2.1

  # Add this only if you are using image or font assets
  cupertino_icons: ^1.0.2

flutter:
  uses-material-design: true

  assets:
    - assets/sheldon.jpg
